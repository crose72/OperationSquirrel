syntax = "proto3";
package os.logger;

import "Common.proto";

message SystemStateMsg {
  Time  t = 1;
  uint32 frame_id  = 2;
  
  int32 system_state     = 10; // cast from your SystemState enum
  float app_elapsed_time = 11; // g_app_elapsed_time (float)
}
